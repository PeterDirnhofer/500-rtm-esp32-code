### PlatformIO: Select COM8 for Download

To use COM8 as the upload port in PlatformIO, add the following line to your `platformio.ini` file:

```
upload_port = COM8
```

Or, when uploading from the command line, use:

```
platformio run --target upload --upload-port COM8
```